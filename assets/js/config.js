// ============================================================
// Diamond Beauty — site configuration
//
// Paste your Supabase project values here (see README.md for
// the 5-minute setup). Until these are filled in, the contact
// and checkout forms fall back to opening an email instead.
//
// These values are SAFE to publish: the anon key only allows
// what the database row-level-security policies permit
// (visitors can submit messages/orders, nothing else).
// ============================================================
window.DIAMOND_CONFIG = {
  SUPABASE_URL: "",      // e.g. "https://abcdefghijkl.supabase.co"
  SUPABASE_ANON_KEY: ""  // the long "anon / public" key
};
